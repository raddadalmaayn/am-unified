# ── Node.js ──────────────────────────────────────────────────────────────────
client-tests/node_modules/
client-tests/.env
dashboard/node_modules/
npm-debug.log*

# ── Go build artefacts ───────────────────────────────────────────────────────
chaincode/unified/unified_ccaas
chaincode/unified/unified
chaincode/unified/*.test

# ── Deployment / packaging artefacts ─────────────────────────────────────────
*.tar.gz
*.zip

# ── Crypto material and network secrets ──────────────────────────────────────
network/organizations/
network/genesis.block
# keep network/config/ (configtx.yaml, crypto-config.yaml — no secrets)

# ── Compiled binaries ─────────────────────────────────────────────────────────
build/

# ── Backup directories ────────────────────────────────────────────────────────
chaincode.backup*/

# ── Node module trees ─────────────────────────────────────────────────────────
sdk-benchmark/node_modules/

# ── OS / editor noise ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# ── Raw evidence: published as tagged release assets, not repository content ──
# Regenerable analysis outputs (analysis.json, occupancy.json, manifests) ARE
# tracked; these two are the bulk per-transaction records behind them.
results/**/txs.jsonl
results/**/resources.csv
results/**/resources.csv.pid
results/_sampler_raw/

# Unrelated manuscript source (A8 audit W1): IEEE TII 2026 draft and journal template assets
Paper/
